<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permisos para Internet y Localización -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ProyectoAndroid">

        <!-- LoginActivity - Launcher principal -->
        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Registro -->
        <activity
            android:name=".RegisterActivity"
            android:exported="false" />

        <!-- Mapa para Usuarios Normales -->
        <activity
            android:name=".NormalUserActivity"
            android:exported="false" />

        <!-- Gestión para Creadores -->
        <activity
            android:name=".CreatorActivity"
            android:exported="false" />

        <!-- MainActivity declarado con exported=true para corregir el error del IDE al lanzar -->
        <activity
            android:name=".MainActivity"
            android:exported="true" />

        <!-- API KEY DE GOOGLE MAPS INSERTADA -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyDTA9ygd0tC6xgozjqr551FAz0W6xt0tHU" />

    </application>

</manifest>